# {{ ansible_managed }}

server {
  listen      80 default_server;
  server_name _;

  location /.well-known/acme-challenge {
    default_type "text/plain";
    root {{ acme_webroot }};
    try_files $uri =404;
  }

  location / {
    return 418;
  }
}
